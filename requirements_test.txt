# Home Assistant core is installed separately in CI
# - stable version: from PyPI (latest stable)
# - dev version: from GitHub (home-assistant/core@dev)

# Component dependencies installed dynamically from HA core:
# - openai_conversation: openai
# - conversation: hassil, home-assistant-intents
# - rest: xmltodict
# - scrape: beautifulsoup4, lxml

# Testing frameworks
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0
pytest-homeassistant-custom-component>=0.12.0

# Schema validation
voluptuous>=0.13.0

# YAML parsing
PyYAML>=6.0
types-PyYAML>=6.0

# HTTP client
httpx>=0.25.0
aiohttp>=3.9.0
